import {Typography} from '../Typography';

const Logo = (): JSX.Element => {
	return (
		<Typography
			size="xl"
			weight="bold"
			letterSpacing="widest"
			textTransform="uppercase"
		>
			Max Kemzi
		</Typography>
	);
};

export default Logo;
